# how to publish react lib 🚀

A project with the purpose of helping to understand the basic process to publish a React lib on NPM.

## Steps

Just follow the commits started with `[step-x]:` from `git log`.
